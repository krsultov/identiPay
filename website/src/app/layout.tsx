import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "identiPay — The Intent-Based Commerce Protocol",
  description:
    "Push-only, intent-based commerce protocol. Human-readable names backed by stealth meta-addresses deliver every payment to a fresh, unlinkable address. Atomic contextual settlement of value and commerce artifacts on Sui.",
  keywords: [
    "identiPay",
    "payments",
    "zero-knowledge",
    "stealth address",
    "blockchain",
    "Sui",
    "commerce protocol",
    "stealth identity",
    "atomic settlement",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
      <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
      <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
      <link rel="manifest" href="/site.webmanifest" />
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
